spring.application.name=spring-basics-rest
server.port=2222

# MySQL connection properties
spring.datasource.url=jdbc:mysql://localhost:3306/pointer?createDatabaseIfNotExist=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=sahu

# Log JPA queries
# Comment this in production
spring.jpa.show-sql=true

# Drop and create new tables (create, create-drop, validate, update)
# Only for testing purposes - comment this in production
spring.jpa.hibernate.ddl-auto=create
